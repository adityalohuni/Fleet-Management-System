# Backend Implementation TODO

This document tracks the progress of the backend implementation for the Fleet Management System.
The backend is built using **Rust**, **Actix-web**, and **SQLx** (PostgreSQL).

## 1. Infrastructure & Configuration
- [ ] **Environment Variables**: Ensure all config is loaded from `.env` (DB URL, JWT Secret, Server Port).
